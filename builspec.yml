version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
  build:
    commands:
      - echo "Building application..."
  post_build:
    commands:
      - echo "Zipping application files..."

artifacts:
  files:
    - '**/*'
  discard-paths: no

deploy:
  commands:
    - echo "Starting deployment..."
    
