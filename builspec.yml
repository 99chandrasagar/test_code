version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - echo "Building application..."
  pre-build:
    commands:
      - echo "pre-build phase"
  build:
    commands:
      - echo "build block"
  post_build:
    commands:
      - echo "post build"

aartifacts:
  files:
    - '**/*'
  name: myname
